export interface PdfRequest {
  customerId: number;
  durninWomersleyBase64Image: string;
  jacksonPollock7Base64Image: string;
  jacksonPollock3Base64Image: string;
  weltmanBase64Image: string;
  navyTapeBase64Image: string;
}
